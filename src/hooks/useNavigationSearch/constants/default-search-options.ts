import { SearchBarProps } from 'react-native-screens';
import { colors } from '@/constants/tokens';

export const defaultSearchOptions: SearchBarProps = {
	tintColor: colors.primary,
	hideWhenScrolling: false,
};
